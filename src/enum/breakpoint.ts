export enum BreakpointEventName {
  SUBMIT = "submit",
  EDIT_INIT = "edit-init",
  CLOSE = "close",
  BREAKPOINT_CHANGED = "breakpoint:changed"
}

export enum BreakpointPauseEventName {
  OPEN = "open",
  CLOSE = "close"
}
